apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: loki
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://grafana.github.io/helm-charts
    chart: loki
    targetRevision: 6.55.0
    helm:
      releaseName: loki
      values: |
        deploymentMode: SingleBinary

        loki:
          auth_enabled: false
          commonConfig:
            replication_factor: 1
          storage:
            type: filesystem
          schemaConfig:
            configs:
              - from: "2024-01-01"
                store: tsdb
                object_store: filesystem
                schema: v13
                index:
                  prefix: loki_index_
                  period: 24h

        singleBinary:
          replicas: 1
          persistence:
            enabled: true
            storageClass: manual
            size: 50Gi

        # Disable scalable-mode components
        write:
          replicas: 0
        read:
          replicas: 0
        backend:
          replicas: 0

        # Disable gateway (use direct service URL in Grafana/Promtail)
        gateway:
          enabled: false

        # Disable built-in Grafana integration (using kube-prometheus-stack's Grafana)
        grafana-agent-operator:
          enabled: false
        monitoring:
          dashboards:
            enabled: true
            labels:
              grafana_dashboard: "1"
          selfMonitoring:
            enabled: false
            grafanaAgent:
              installOperator: false
          lokiCanary:
            enabled: false

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
